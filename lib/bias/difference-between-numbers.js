'use strict';

/**
 * Adds commas to a number
 * @param {number} firstNumber
 * @param {number} secondNumber
 * @return {string}
 */

module.exports = function(firstNumber, secondNumber) {

	return Math.abs( (firstNumber * 10) - (secondNumber * 10) );

};

